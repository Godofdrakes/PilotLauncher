<Window x:Class="PilotLauncher.PropertyGrid.WPF.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:propertyGrid="clr-namespace:PilotLauncher.PropertyGrid;assembly=PilotLauncher.PropertyGrid"
	mc:Ignorable="d"
	DataContext="{Binding RelativeSource={RelativeSource Self}}"
	Title="MainWindow" Height="450" Width="800">
	<propertyGrid:PropertyGridView x:Name="PropertyGridView"
		PropertySource="{Binding Example}"
		PropertyItemAdded="PropertyGridView_OnPropertyItemAdded"
		ShowPropertyName="{Binding Example.IsNameVisible, Mode=OneWay}"
		ShowPropertyType="{Binding Example.IsTypeVisible, Mode=OneWay}" />
</Window>